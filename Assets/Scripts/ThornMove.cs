using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//I hoped to have it so you could pick thorns off the crown and stick it in the nose of the face.
//The project took significantly longer than I expected, so I didn't get to this. I'm
//keeping this script in here just for the note, though.
public class ThornMove : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
